# Give your module a name
name: zmkdriverazoteq

build:
  # List your dependencies
  # depends:
  #   - zmk-driver-azoteq
  # Folder containing a "CMakeLists.txt" (. = root)
  cmake: .
  # Kconfig filepath
  kconfig: Kconfig
  settings:
    # Folder containing a "dts" folder (. = root)
    dts_root: ./dts
